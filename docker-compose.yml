services:
  server:
    build: .
    container_name: rss-reader
    restart: always
    ports:
      - "8081:8080"
    volumes:
      - "$PWD/config.json:/app/config.json"
      - "$PWD/data:/app/data"
    stop_grace_period: 10s
